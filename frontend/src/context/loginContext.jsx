//cause the react was not rendering the nav bar so we use context
import React, {createContext} from "react";

export const LoginContext = createContext()

// context is an API of react which let you create  global variable